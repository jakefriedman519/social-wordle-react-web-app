export default function Tournaments() {
  // TODO we would need to check roles here, show all tournaments for admins
  // we would also need to have a tournament creation page, and a tournament join page / do wordle tournament where u could search for tournaments
  // we would also have to check if the current user is the tournament creator
  // /tournaments/:tournamentId -> check role here
  // /tournaments/:tournamentId/leaderboard
  // /tournaments/create
  return (
    <div>
      <h1>Tournaments</h1>
      <p>TODO: Tournaments page</p>
    </div>
  );
}
